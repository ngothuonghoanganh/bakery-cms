export enum StockItemStatus {
  AVAILABLE = 'available',
  LOW_STOCK = 'low_stock',
  OUT_OF_STOCK = 'out_of_stock',
}

export enum MovementType {
  RECEIVED = 'received',
  USED = 'used',
  ADJUSTED = 'adjusted',
  DAMAGED = 'damaged',
  EXPIRED = 'expired',
}
