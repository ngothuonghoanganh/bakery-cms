import { Form } from 'antd';

// Simple re-export of Form and its sub-components
export const AntForm = Form;
export const AntFormItem = Form.Item;
export const AntFormList = Form.List;
export const AntFormProvider = Form.Provider;
