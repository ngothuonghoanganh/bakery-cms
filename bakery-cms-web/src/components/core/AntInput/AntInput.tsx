import { Input } from 'antd';

// Re-export Input and its sub-components
export const AntInput = Input;
export const AntTextArea = Input.TextArea;
export const AntInputPassword = Input.Password;
export const AntInputSearch = Input.Search;
