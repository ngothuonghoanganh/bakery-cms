import { Select } from 'antd';

// Re-export Select and its sub-components
export const AntSelect = Select;
export const AntSelectOption = Select.Option;
export const AntSelectOptGroup = Select.OptGroup;
